// NUMBERS
// INTEGERS
let score = 10

// In Swift, we can use underscored to break up long numbers for better readability
let reallyBig = 100_000_000

// We can perform simple arithmetic operations with numbers in Swift
let lowerScore = score - 2

let higherScore = score + 10

let doubledScore = score * 2

let squaredScore = score * score

let halvedScore = score / 2

// We make use of COMPOUND ASSIGNMENT OPERATORS to perform arithmetic operations / calculations on the counter variable below with less code or typing
var counter = 10
counter += 5
print(counter)

counter *= 2
counter -= 10
counter /= 2

// PROPERTIES AND METHODS
let number = 120

// The number.isMultiple(of: Int) method checks if the number is a multiple of another integer
print(number.isMultiple(of: 3))